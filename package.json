{
  "name": "cambiar-nombres-videos",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
    "scripts": {
    "dev": "bun run cambiar-nombres-videos.ts",
    "test": "bun test",
    "exe": "bun build cambiar-nombres-videos.ts --compile --outfile exe/cambiar-nombres-videos.exe --minify --bytecode"
  }
}
